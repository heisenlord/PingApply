
📧 Auto-Fill Chrome Extension
This Chrome extension automates the process of filling out and submitting login forms by autofilling email and password fields and clicking the necessary buttons (e.g., "Next", "Submit", "Login").

🌟 Features
Automatically fills in the email and password fields on login forms.
Clicks the "Next" and "Login" buttons for you, simplifying form submission.
Handles delays and ensures elements are loaded before proceeding.
📋 Prerequisites
Google Chrome browser
Basic knowledge of Chrome extensions
🚀 How to Install
Clone the repository:

bash
Copy code
git clone https://github.com/your-username/your-repo-name.git
Open Chrome and navigate to chrome://extensions/.

Enable Developer Mode (toggle it on in the upper right corner).

Click Load unpacked and select the folder where the repository is cloned.

Your extension will now be installed and available in your Chrome extensions.

🛠️ Usage
Click the extension icon in Chrome's toolbar.
Enter the email and password you want to autofill into the form.
The extension will fill in the email and password fields, click the "Next" button, and submit the form automatically.
🔧 Message Listener
The extension listens for a message event to trigger the autofill process:

javascript
Copy code
chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
    if (request.action === 'autofill') {
        autofillAndSubmit(request.email, request.password);
    }
});
💻 Example
To trigger the autofill:

javascript
Copy code
chrome.runtime.sendMessage({
    action: 'autofill',
    email: 'youremail@example.com',
    password: 'yourpassword'
});
🐛 Debugging
Use Chrome DevTools to inspect the background script and content scripts.
Check the console for any errors or log outputs during the form-filling process.
💡 Contribution
Feel free to submit pull requests for improvements or open issues for bugs!

This README provides a clear explanation of the project, how to install and use it, and details on its main features. 🛠
